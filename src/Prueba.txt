(setq a 10)

(setq b 11)

(setq operacion '+')

( + a b )

(cond ((equals operacion '+') ( + a b ))
        ((equals operacion '-') ( - a b ))
        ((equals operacion '*') ( * a b ))
        ((equals operacion '/') ( / a b ))
        (t "Operación inválida"))